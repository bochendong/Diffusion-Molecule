\section{Results}
\label{sec:results}

We organize the main results around three questions. First, how does
MolProgram compare with prior methods under established multi-property
benchmarks? Second, does group-relative post-training improve direct
generation at practical candidate budgets rather than only under large
best-of-$k$ search? Third, does the gain persist as the number of simultaneous
constraints increases? Unless stated otherwise, MolProgram uses one frozen
evaluation seed, preserves generation order, and never reports a
property-reranked candidate as one-shot generation.

\subsection{Comparison with prior methods}
\label{sec:results-prior}

We first align source-conditioned editing with the complete MolEdit-Instruct
comparison rather than reporting only a macro-average. Table~\ref{tab:moledit-full}
retains all ten tasks and the five metrics used in our comparison: validity
and overall/valid accuracy at two source-similarity thresholds. Following the benchmark
contract, every method generates 20 molecules per input and accuracy is
computed candidate-wise over the complete unranked pool. Raw@1 and pass@$k$
are budget diagnostics and are reported separately rather than mixed into the
external comparison. The completed P4 run does not pass its promotion gate:
on its 20-input-per-task pilot, the final Group-RL student obtains zero
$\operatorname{Acc}_{\mathrm{all}}(.65)$, 32.5\% mean validity, and 7.5\% mean
$\operatorname{Acc}_{\mathrm{all}}(.15)$. Because this pilot is smaller than
the published benchmark evaluation, it is reported in the appendix and does
not populate the P4 row. Distillation preserves the source-free de novo path
but does not make direct SMILES decoding a competitive source-preserving
editor. We omit FCD because it is outside the present evaluation contract.

\input{moledit_table1.tex}

We then evaluate zero-source generation under the SketchMol multi-property
protocol. For each of 1,000 held-out property vectors at each complexity level,
a method receives 40 generation attempts and succeeds if at least one valid
molecule satisfies every active property within the benchmark tolerance.
Table~\ref{tab:sketchmol-fair40} defines a matched comparison spanning
property--SMILES foundation models, autoregressive structural generators,
graph diffusion, image diffusion, and our direct-SMILES policy
\citep{chang2024spmm,jolicoeurmartineau2024stggplus,liu2024graphdit,wang2025sketchmol}.
We report only completed evaluations: external rows remain blank until they
are rerun on the identical targets rather than importing numbers from an
incompatible dataset or tolerance. Among the two completed rows, MolProgram
is substantially stronger on two- and three-property programs and remains
within 0.9 points at four properties. SketchMol is stronger from five
properties onward and leads the overall average by 5.0 points. The present
comparison therefore identifies high-order composition as the principal gap,
but does not yet establish an overall state-of-the-art result.

\begin{table*}[t]
  \centering
  \caption{External comparison planned under the SketchMol multi-property
  de novo protocol. Strict success (\%) uses the same 1,000 targets per
  complexity, property tolerances, and 40 candidates per condition. The
  SketchMol row is the published matched reference; MolProgram is evaluated
  on the same target file. Dashes mark matched reruns that are pending, not
  reported failures. Bold marks the best completed result.}
  \label{tab:sketchmol-fair40}
  \scriptsize
  \setlength{\tabcolsep}{3.6pt}
  \begin{tabular}{lllrrrrrrr}
    \toprule
    Method & Molecular representation & Evaluation status & Avg. & 2p & 3p & 4p & 5p & 6p & 7p \\
    \midrule
    SPMM & property vector + SMILES & matched rerun pending
      & -- & -- & -- & -- & -- & -- & -- \\
    STGG+ & spanning-tree sequence & matched rerun pending
      & -- & -- & -- & -- & -- & -- & -- \\
    Graph DiT & molecular graph & matched rerun pending
      & -- & -- & -- & -- & -- & -- & -- \\
    SketchMol & molecular image & published matched reference
      & \textbf{73.1} & 80.4 & 76.8 & \textbf{73.6} & \textbf{71.6} & \textbf{67.8} & \textbf{68.5} \\
    \midrule
    MolProgram (Group-RL) & direct SMILES & ours, matched rerun
      & 68.1 & \textbf{91.7} & \textbf{83.2} & 72.7 & 63.6 & 51.0 & 46.5 \\
    \bottomrule
  \end{tabular}
\end{table*}

We next use MuMOInstruct to expose source-conditioned composition at the task
level. Tables~\ref{tab:mumo-ind-full} and~\ref{tab:mumo-ood-full} retain the
benchmark's five IND and five OOD task families and report success rate (SR),
source similarity among successes (Sim), and relative improvement among
successes (RI), rather than collapsing each split to one average
\citep{dey2025gellm3o}. This layout separates general-purpose LLMs,
chemistry-foundation models, task-specific systems, and generalist
instruction-tuned models. The frozen P4 row remains blank until all three
official metrics are exported for every task under the same 20-candidate
contract. Aggregate property success without the corresponding source
fidelity is retained only as a diagnostic in
Appendix~\ref{sec:appendix-mumo}.

\input{mumo_tables.tex}

\subsection{Group-relative post-training improves practical-budget generation}
\label{sec:results-budget}

Table~\ref{tab:budget-scaling} compares SFT and Group-RL on 6,000
two-to-seven-property programs and 1,000 OOD programs. The Group-RL gain is
already visible with small candidate groups. On 2p--7p, first-draw strict
success increases from 7.02\% to 8.93\%, while Pass@8 and Pass@20 improve by
6.35 and 7.64 points, respectively. OOD is more nuanced: SFT is slightly
better on the first draw, but Group-RL overtakes it by $k=8$ and improves
Pass@20 from 35.20\% to 41.70\%. The benefit is therefore not a
best-of-256-only effect, although it is not uniform at $k=1$.

\begin{table}[t]
  \centering
  \caption{Budget-transparent direct generation. Raw strict is the
  candidate-level strict-success fraction; Pass@$k$ is the percentage of
  conditions solved at least once within the first $k$ draws.}
  \label{tab:budget-scaling}
  \scriptsize
  \setlength{\tabcolsep}{3.5pt}
  \begin{tabular}{lrrrrrr}
    \toprule
    Evaluation & $k$ & \multicolumn{2}{c}{Raw strict (\%)} & \multicolumn{3}{c}{Pass@$k$ (\%)} \\
    \cmidrule{3-4}\cmidrule{5-7}
    & & SFT & Group-RL & SFT & Group-RL & $\Delta$ \\
    \midrule
    2p--7p & 1  & 7.02 & \textbf{8.93} & 7.02 & \textbf{8.93} & +1.91 \\
    2p--7p & 8  & 7.26 & \textbf{9.28} & 35.37 & \textbf{41.72} & +6.35 \\
    2p--7p & 20 & 7.30 & \textbf{9.41} & 54.88 & \textbf{62.52} & +7.64 \\
    \midrule
    OOD & 1  & \textbf{3.50} & 3.20 & \textbf{3.50} & 3.20 & -0.30 \\
    OOD & 8  & 3.20 & \textbf{3.78} & 19.70 & \textbf{23.60} & +3.90 \\
    OOD & 20 & 3.14 & \textbf{3.79} & 35.20 & \textbf{41.70} & +6.50 \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{The gain persists with increasing program complexity}
\label{sec:results-complexity}

Absolute success decreases monotonically with program length for both models,
confirming that property count exposes a genuine compositional difficulty
axis. Nevertheless, Table~\ref{tab:complexity-scaling} shows that Group-RL
improves every property count at both practical budgets. At $k=20$, it adds
4.9 points on 6p and 8.4 points on 7p programs; the largest improvement is
11.8 points on 5p. Thus the aggregate gain cannot be explained by easy
two-property conditions dominating the benchmark.

\begin{table}[t]
  \centering
  \caption{Compositional scaling on the 2p--7p benchmark. Values are the
  percentage of programs solved within the indicated candidate budget.}
  \label{tab:complexity-scaling}
  \small
  \setlength{\tabcolsep}{5pt}
  \begin{tabular}{crrrrrr}
    \toprule
    & \multicolumn{3}{c}{Pass@8} & \multicolumn{3}{c}{Pass@20} \\
    \cmidrule{2-4}\cmidrule{5-7}
    Properties & SFT & Group-RL & $\Delta$ & SFT & Group-RL & $\Delta$ \\
    \midrule
    2 & 58.6 & \textbf{68.1} & +9.5 & 78.6 & \textbf{85.6} & +7.0 \\
    3 & 45.4 & \textbf{52.6} & +7.2 & 66.0 & \textbf{73.7} & +7.7 \\
    4 & 35.7 & \textbf{42.9} & +7.2 & 60.3 & \textbf{66.3} & +6.0 \\
    5 & 30.2 & \textbf{36.1} & +5.9 & 48.7 & \textbf{60.5} & +11.8 \\
    6 & 23.1 & \textbf{27.1} & +4.0 & 42.6 & \textbf{47.5} & +4.9 \\
    7 & 19.2 & \textbf{23.5} & +4.3 & 33.1 & \textbf{41.5} & +8.4 \\
    \bottomrule
  \end{tabular}
\end{table}

Together, these results show a consistent but bounded advantage: Group-RL
raises small-budget success across the full compositional axis, while
high-order de novo generation, raw SMILES validity, and source preservation
remain open limitations. Full validity diagnostics, negative ablations,
MolEdit-Instruct comparisons, and paired decoder/editing repairs are reported
in the appendix rather than mixed into the main result narrative. In
particular, syntax-safe decoding substantially improves raw validity and
low-budget OOD success, but does not meet its preregistered promotion gate or
repair the small high-order OOD subset.
