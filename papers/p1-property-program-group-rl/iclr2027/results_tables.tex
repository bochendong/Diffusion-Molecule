\section{Results}
\label{sec:results}

We organize the results around three questions. First, does group-relative
post-training improve compositional property control at practical candidate
budgets rather than only under large best-of-$k$ search? Second, does the gain
persist as property programs become longer and move out of distribution? Third,
can de novo generation and source-conditioned editing share a property-program
and reinforcement interface while using transition operators appropriate to
their different molecular states?
Unless stated otherwise, all
candidate streams preserve generation order, Raw@1 is exactly the first model
draw, and property-reranked selection is never reported as one-shot generation.
All experiments use the frozen single seed specified before evaluation.

Table~\ref{tab:framework-overview} summarizes the completed evidence across
the two initial-state regimes. Every MolProgram row uses the same typed
property-program contract, programmatic verification interface, and
within-condition group-relative learning principle; the transition operator is
the state-dependent component. The table keeps condition-level Pass@$k$ and
candidate-level editing accuracy visibly separate.

\begin{table*}[t]
  \centering
  \caption{MolProgram across molecular construction and editing. SFT and GRPO
  are evaluated under the same candidate stream within each row. De novo
  Pass@$k$ is condition-level; editing accuracy averages every unranked
  candidate. Strict and relaxed editing denote
  $\operatorname{Acc}_{\rm all}(.65)$ and
  $\operatorname{Acc}_{\rm all}(.15)$, respectively. Published references are
  included only under an identical metric. Bold denotes the best completed
  value in a row.}
  \label{tab:framework-overview}
  \scriptsize
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{lllrrrr}
    \toprule
    Initial graph & Operator & Metric & SFT & GRPO & $\Delta$ & Published ref. \\
    \midrule
    Empty (2p--7p) & Direct SMILES & Pass@8 & 35.37 & \textbf{41.72} & +6.35 & -- \\
    Empty (2p--7p) & Direct SMILES & Pass@20 & 54.88 & \textbf{62.52} & +7.64 & -- \\
    Empty (OOD) & Direct SMILES & Pass@20 & 35.20 & \textbf{41.70} & +6.50 & -- \\
    Source (MolEdit) & Graph-event kernel & Cand. strict
      & 43.35 & \textbf{46.12} & +2.77 & MolEditRL: 45.00 \\
    Source (MolEdit) & Graph-event kernel & Cand. relaxed
      & 57.60 & 61.90 & +4.30 & \textbf{MolEditRL: 72.70} \\
    \bottomrule
  \end{tabular}
\end{table*}

\subsection{Group-relative post-training improves low-budget de novo design}
\label{sec:results-budget}

Table~\ref{tab:budget-scaling} compares supervised fine-tuning (SFT) and
GRPO on 6,000 two-to-seven-property programs and 1,000
out-of-distribution (OOD) programs. The improvement is already visible at
small candidate budgets. On 2p--7p, strict success on the first draw increases
from 7.02\% to 8.93\%; Pass@8 and Pass@20 increase by 6.35 and 7.64 percentage
points. The OOD first draw is deliberately reported as a negative result: SFT
is 0.30 points better. GRPO nevertheless overtakes SFT by $k=8$ and raises
Pass@20 from 35.20\% to 41.70\%. The observed advantage is therefore not a
$k=256$-only effect, but neither is it a uniform improvement in one-shot
generation.

\begin{table}[t]
  \centering
  \caption{Budget-transparent direct de novo generation. Raw strict is the
  candidate-level strict-success fraction; Pass@$k$ is the percentage of
  property programs solved at least once within the first $k$ model draws.
  Both variants use MolProgram's direct-SMILES transition operator.}
  \label{tab:budget-scaling}
  \scriptsize
  \setlength{\tabcolsep}{3.5pt}
  \begin{tabular}{lrrrrrr}
    \toprule
    Evaluation & $k$ & \multicolumn{2}{c}{Raw strict (\%)} & \multicolumn{3}{c}{Pass@$k$ (\%)} \\
    \cmidrule{3-4}\cmidrule{5-7}
    & & SFT & GRPO & SFT & GRPO & $\Delta$ \\
    \midrule
    2p--7p & 1  & 7.02 & \textbf{8.93} & 7.02 & \textbf{8.93} & +1.91 \\
    2p--7p & 8  & 7.26 & \textbf{9.28} & 35.37 & \textbf{41.72} & +6.35 \\
    2p--7p & 20 & 7.30 & \textbf{9.41} & 54.88 & \textbf{62.52} & +7.64 \\
    \midrule
    OOD & 1  & \textbf{3.50} & 3.20 & \textbf{3.50} & 3.20 & -0.30 \\
    OOD & 8  & 3.20 & \textbf{3.78} & 19.70 & \textbf{23.60} & +3.90 \\
    OOD & 20 & 3.14 & \textbf{3.79} & 35.20 & \textbf{41.70} & +6.50 \\
    \bottomrule
  \end{tabular}
\end{table}

Raw validity explains part, but not all, of this change. Across the matched
2p--7p stream, GRPO raises candidate validity from 29.04\% to 34.25\% at
$k=20$ while also increasing the fraction of valid molecules that satisfy all
active clauses. OOD validity remains low and nearly unchanged (11.51\% versus
11.60\%), even though OOD Pass@20 improves by 6.50 points. Thus the OOD gain
comes from reallocating probability among the small valid subset, rather than
from solving molecular syntax in general. Full validity and uniqueness
diagnostics are reported in Appendix~\ref{sec:appendix-validity}.

\subsection{The gain persists as programs become more compositional}
\label{sec:results-complexity}

Absolute success decreases with program length for both policies, confirming
that property count defines a genuine compositional difficulty axis.
Table~\ref{tab:complexity-scaling} shows, however, that GRPO improves every
property count at both practical budgets. At $k=20$, the gain is 4.9 points on
6p programs and 8.4 points on 7p programs; the largest increase, 11.8 points,
occurs at 5p. The aggregate improvement therefore cannot be explained by easy
two-property requests dominating the benchmark.

\begin{table}[t]
  \centering
  \caption{Compositional scaling on the 2p--7p benchmark. Values are the
  percentage of programs solved within the indicated candidate budget.}
  \label{tab:complexity-scaling}
  \small
  \setlength{\tabcolsep}{5pt}
  \begin{tabular}{crrrrrr}
    \toprule
    & \multicolumn{3}{c}{Pass@8} & \multicolumn{3}{c}{Pass@20} \\
    \cmidrule{2-4}\cmidrule{5-7}
    Properties & SFT & GRPO & $\Delta$ & SFT & GRPO & $\Delta$ \\
    \midrule
    2 & 58.6 & \textbf{68.1} & +9.5 & 78.6 & \textbf{85.6} & +7.0 \\
    3 & 45.4 & \textbf{52.6} & +7.2 & 66.0 & \textbf{73.7} & +7.7 \\
    4 & 35.7 & \textbf{42.9} & +7.2 & 60.3 & \textbf{66.3} & +6.0 \\
    5 & 30.2 & \textbf{36.1} & +5.9 & 48.7 & \textbf{60.5} & +11.8 \\
    6 & 23.1 & \textbf{27.1} & +4.0 & 42.6 & \textbf{47.5} & +4.9 \\
    7 & 19.2 & \textbf{23.5} & +4.3 & 33.1 & \textbf{41.5} & +8.4 \\
    \bottomrule
  \end{tabular}
\end{table}

The OOD split contains reversed directions, extreme targets, and property
combinations that are rare or absent during training. GRPO improves the
aggregate OOD stream from $k=8$ onward, but the improvement is not uniform
across OOD strata. In particular, a syntax-safe decoder raises overall OOD
validity and Pass@20 but regresses on the small OOD 7p subset. We therefore
interpret the completed evidence as improved compositional sampling, not as a
claim that high-order extrapolation or validity has been solved.

\subsection{Comparison with matched de novo generation}
\label{sec:results-denovo-external}

Table~\ref{tab:sketchmol-fair40} places the direct MolProgram policy in the
SketchMol multi-property protocol. Each method receives 40 attempts for each
of 1,000 held-out target vectors at every complexity, and a condition succeeds
when at least one valid molecule satisfies every active tolerance. We import
only the published SketchMol row because it uses the matched target and
tolerance contract. Methods whose published results use different targets,
oracles, or budgets are omitted rather than displayed as empty rows or compared
through incompatible protocols. MolProgram is stronger on 2p and 3p and is
within 0.9 points at 4p. SketchMol leads from 5p onward and by 5.0 points on the
overall average. The external comparison therefore identifies high-order
generation as the primary remaining de novo gap and does not support an overall
state-of-the-art claim.

\begin{table*}[t]
  \centering
  \caption{Matched comparison under the SketchMol multi-property de novo
  protocol. Strict success (\%) uses 1,000 targets per complexity, the same
  property tolerances, and 40 candidates per condition. Only completed results
  under this exact protocol are included. Bold marks the best value.}
  \label{tab:sketchmol-fair40}
  \scriptsize
  \setlength{\tabcolsep}{3.6pt}
  \begin{tabular}{llrrrrrrr}
    \toprule
    Method & Molecular representation & Avg. & 2p & 3p & 4p & 5p & 6p & 7p \\
    \midrule
    SketchMol & molecular image
      & \textbf{73.1} & 80.4 & 76.8 & \textbf{73.6} & \textbf{71.6} & \textbf{67.8} & \textbf{68.5} \\
    \midrule
    MolProgram (direct GRPO) & direct SMILES
      & 68.1 & \textbf{91.7} & \textbf{83.2} & 72.7 & 63.6 & 51.0 & 46.5 \\
    \bottomrule
  \end{tabular}
\end{table*}

\subsection{Shared programs benefit from state-appropriate transition operators}
\label{sec:results-transition-audit}

Forcing construction and editing through one checkpoint is attractive but not
supported by the matched evidence. The single-checkpoint ablation preserves
the direct de novo policy yet reaches only 17.075\% strict
candidate-level accuracy on its 200-condition editing gate. We therefore share
the property-program and verifier layers while assigning the empty and
populated boundary conditions different transition operators. The promoted
editor is a source-anchored, validity-preserving graph-event kernel; it samples
exactly 20 raw trajectories per condition without a task router, molecular
ranker, inference oracle, second edit, or MCS glue.

Table~\ref{tab:operator-split} reports both candidate-level accuracy and
Any@20 rather than conflating them. On 997 held-out conditions and 19,940 raw
candidates, the supervised event editor reaches 43.35\% strict candidate
accuracy. Applying
the same group-relative learning principle used by the direct constructor
raises strict accuracy by 2.77 points to 46.12\%, exceeding MolEditRL's
reported 45.0\% task macro. The relaxed result remains lower than MolEditRL
(61.90\% versus 72.7\%), so the gain is specific to source-preserving strict
editing rather than an across-the-board state-of-the-art claim.

\begin{table}[t]
  \centering
  \caption{MolEdit Table 1 macro results under two aggregation rules. Candidate
  accuracy averages over every unranked output and is directly comparable to
  MolEditRL; Any@20 asks whether a condition has at least one hit. MolProgram
  uses one frozen seed and exactly 20 raw candidates per condition.}
  \label{tab:operator-split}
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{lrrrrr}
    \toprule
    Editor & Validity & Cand. strict & Cand. relaxed & Any@20 strict & Any@20 relaxed \\
    \midrule
    Single-checkpoint ablation & 100.00 & 17.08 & 18.03 & 70.50 & 71.00 \\
    MolProgram event SFT & 97.38 & 43.35 & 57.60 & 76.52 & 92.51 \\
    MolProgram event GRPO & \textbf{97.51} & \textbf{46.12} & 61.90 & \textbf{78.83} & \textbf{93.31} \\
    MolEditRL (reported candidate macro) & 96.62 & 45.00 & \textbf{72.70} & -- & -- \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Source-conditioned editing against published baselines}
\label{sec:results-editing}

Table~\ref{tab:moledit-full} retains the complete MolEdit-Instruct comparison:
ten tasks, validity, and overall/valid-conditional accuracy at source
similarity thresholds 0.65 and 0.15. The published rows retain their reported
candidate-level metrics. Our event-editor rows use exactly 20 generated
trajectories per condition and are evaluated candidate-wise over the complete
unranked pool.
This is intentionally different from Pass@$k$, Any@$k$, or a
property-selected output. The single-checkpoint result is retained only as an
ablation; event GRPO is the promoted editing operator. We omit FCD because
it is not part of our evaluation contract.

\input{moledit_table1.tex}

The per-task table exposes a heterogeneous gain. Event GRPO is strongest on
Rotbonds$\downarrow$, SA$\downarrow$, and four two-property tasks, but remains
weak on GSK3B$\uparrow$, MW$\uparrow$, and the final three-property request.
Macro $\operatorname{Acc}_{\rm all}(.65)$ nevertheless reaches 46.12\%, 1.12
points above MolEditRL, while macro $\operatorname{Acc}_{\rm all}(.15)$ reaches
61.90\%, 10.80 points below MolEditRL. Candidate validity is 97.51\%. The same
raw stream reaches 78.83\% strict Any@20, but this answers whether a condition
has at least one hit and is never substituted for candidate-level accuracy.

\subsection{MuMOInstruct IND and OOD task families}
\label{sec:results-mumo}

Tables~\ref{tab:mumo-ind-full} and~\ref{tab:mumo-ood-full} preserve the five IND
and five OOD MuMOInstruct task families and their three complementary metrics:
success rate (SR), similarity among successful outputs (Sim), and relative
improvement among successes (RI) \citep{dey2025gellm3o}. This fuller layout is
necessary because a high property SR can coexist with very low source
similarity. The MolProgram cells remain blank until all official
oracles and source-fidelity metrics are exported from the same 20-candidate
run.

\input{mumo_tables.tex}

Taken together, the completed results support two bounded conclusions.
GRPO increases the probability that a modest direct
sample set solves a compositional property program, including at 6p, 7p, and
on aggregate OOD requests. Sharing the property-program and verifier layers
while specializing the molecular transition operator avoids the severe editing
loss of a forced single-checkpoint design. The source-anchored operator exceeds
MolEditRL under strict candidate-level accuracy after GRPO, but not under
the relaxed threshold. We therefore claim a unified request and learning
framework with state-appropriate solvers, not a universal decoder or uniform
dominance across editing criteria.
