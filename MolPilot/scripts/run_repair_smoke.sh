#!/bin/bash
set -euo pipefail

cd "$(dirname "${BASH_SOURCE[0]}")/.."

export MOLPILOT_TASK_MODE=repair
export MOLPILOT_RUN_NAME="${MOLPILOT_RUN_NAME:-repair_smoke}"
export MOLPILOT_LIMIT="${MOLPILOT_LIMIT:-0}"
export MOLPILOT_REPAIR_CORRUPTIONS_PER_MOLECULE="${MOLPILOT_REPAIR_CORRUPTIONS_PER_MOLECULE:-3}"
export MOLPILOT_EVAL_TASKS="${MOLPILOT_EVAL_TASKS:-repair}"
export MOLPILOT_SAMPLES="${MOLPILOT_SAMPLES:-3}"
export MOLPILOT_DECODE_TOP_K="${MOLPILOT_DECODE_TOP_K:-4}"
export MOLPILOT_AE_EPOCHS="${MOLPILOT_AE_EPOCHS:-2}"
export MOLPILOT_ALIGN_EPOCHS="${MOLPILOT_ALIGN_EPOCHS:-2}"
export MOLPILOT_DIFFUSION_EPOCHS="${MOLPILOT_DIFFUSION_EPOCHS:-2}"
export MOLPILOT_TIMESTEPS="${MOLPILOT_TIMESTEPS:-20}"

bash scripts/run_staged_smoke.sh
